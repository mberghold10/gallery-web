pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r * public/ || true
    - rm -rf public/public  # avoid nested copy
    - cp CNAME public/CNAME
  artifacts:
    paths:
      - public
  only:
    - main
